#ifndef EXPONENTIALDENSITY _H
#define EXPONENTIALDENSITY _H

#include <math.h>

class ExponentialDensity
{
    public:
        ExponentialDensity ();
        static double Evaluate();

    protected:

    private:
};

#endif // EXPONENTIALDENSITY _H
